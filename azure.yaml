# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: barberslop
services:
  app:
    language: dotnet
    project: ./src/Barberslop.AppHost/Barberslop.AppHost.csproj
    host: containerapp
